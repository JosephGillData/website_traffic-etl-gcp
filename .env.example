# Google Cloud Project Configuration
# ----------------------------------
# Your GCP project ID (find it in Google Cloud Console)
GCP_PROJECT=your-gcp-project-id

# Google Cloud Storage bucket name (must exist)
GCS_BUCKET=your-gcs-bucket-name

# BigQuery dataset name (must exist in your project)
BQ_DATASET=your_bq_dataset

# BigQuery table name (will be created if it doesn't exist)
BQ_TABLE=traffic_data

# Path to the source XLS file (relative or absolute)
LOCAL_XLS_PATH=data/traffic_spreadsheet.xls

# Path to your GCP service account credentials JSON file
# Download this from: GCP Console > IAM & Admin > Service Accounts
GOOGLE_APPLICATION_CREDENTIALS=path/to/your-service-account-key.json

# Optional: BigQuery write disposition
# - "append" (default): Add new rows to existing data
# - "truncate": Replace all existing data with new data
BQ_WRITE_DISPOSITION=append
